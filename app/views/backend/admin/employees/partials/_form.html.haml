= form_for([:admin ,employee ] , remote: true, html: {class: 'ajax validate' ,role: :form, 'data-model': 'employee'}) do |form|
  .modal-body
    = render 'backend/admin/employees/partials/fields' , f: form
  .modal-footer
    = form.button "Submit", class: "btn btn-large btn-danger", "data-disable-with" => "Submitting.."
    %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} Close